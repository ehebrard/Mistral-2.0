From within the folder Mistral-2.0:

To build (i.e., generate the executable fzn-mistral):
$ make

Options: 
fzn-mistral does not take annotations into account, and does not use parallel threads, so the options -a, -f and -p have no use (yet).







